export interface TVShow {
    id: number;
    name: string;
}

export interface TVShow {
    id: number;
    name: string;
    summary: string;
    image: {
        medium: string;
        original: string;
    };
}